<template>
  <div>
    <h3>Playlists</h3>
    <div v-if="loading">Loading playlists...</div>
    <div v-if="error" style="color:red">{{ error }}</div>

    <ul v-if="playlists.length">
      <li v-for="pl in playlists" :key="pl.id">
        {{ pl.title }} - {{ pl.track_count || '?' }} tracks
      </li>
    </ul>
    <div v-else-if="!loading">No playlists found.</div>
  </div>
</template>

<script>
export default {
  props: ['playlists', 'loading', 'error']
}
</script>
