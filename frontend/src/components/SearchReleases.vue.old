<template>
  <div>
    <h3>Releases</h3>
    <div v-if="loading">Loading releases...</div>
    <div v-if="error" style="color:red">{{ error }}</div>

    <ul v-if="releases.length">
      <li v-for="rel in releases" :key="rel.id">
        {{ rel.title }} by {{ rel.artist }} ({{ rel.date }})
        <button @click="$emit('fetch-tracks', rel.id)">Tracks</button>
        <button @click="$emit('fetch-cover', rel.id)">Cover</button>
      </li>
    </ul>
    <div v-else-if="!loading">No releases found.</div>
  </div>
</template>

<script>
export default {
  props: ['releases', 'loading', 'error']
}
</script>
